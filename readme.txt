Do we want duplicates during extraction 
Find way to prevent adding extra whens run running program twice
tested pre_computed kmers with the naive checker, and number of precomputed values with math (n choose 2 * 3 * 3 two diffs) + (n * 3 one diffs) + 1 original
